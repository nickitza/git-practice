source "https://rubygems.org"
#gemfiles need a source, ^ most popular
#can also run bundle init to create gemfile

gem "colorize"
#run bundle install to install the gems in the gemfile
#will creat gemfile.lock that shows the bundle installed